---
import RedirectPage from '@/components/pages/RedirectPage.astro';
import { type Locale, getLocalizedPath } from '@/i18n';
import { createLocaleOnlyStaticPaths } from '@/lib/route-helpers';

export const getStaticPaths = createLocaleOnlyStaticPaths;

const { locale } = Astro.props as { locale: Locale };
---

<RedirectPage to={getLocalizedPath('/photos', locale)} />
