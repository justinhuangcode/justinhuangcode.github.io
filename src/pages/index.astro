---
import BlogIndexPage from '@/components/pages/BlogIndexPage.astro';
import { getPaginatedPosts } from '@/lib/site-content';

const { posts, totalPages } = await getPaginatedPosts('en', 1);
---

<BlogIndexPage locale="en" currentPage={1} posts={posts} totalPages={totalPages} />
